linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused
    - misspell
    - gofmt
    - goimports

run:
  timeout: 5m

linters-settings:
  goimports:
    local-prefixes: github.com/maxvanp/terraform-provider-graviteeam
